const cds = require("@sap/cds");
const { Tasks } = cds.entities('my.project');

module.exports = AdminService = (srv) => {

    //Insert a new task
    // srv.on("CREATE","Tasks",(req,res) => {
    //     const task = req.data
    //     console.log(task);
    // })
}